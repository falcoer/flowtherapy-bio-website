{
  "name": "flowtherapy-contact-worker",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test", "deploy": "wrangler deploy", "check": "wrangler deploy --dry-run" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
